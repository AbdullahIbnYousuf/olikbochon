# V7 Compatible Hybrid Implementation Plan

1. Freeze stable row IDs, corrected context routing, the strengthened duplicate graph, and the
   3x5 common outer-fold assignments. Generate nested inner folds deterministically from only
   each outer-training partition.
2. Port only the audited V4/V5 preprocessing, diagnostics, exact rule, Candidate I sparse model,
   and required grouping ideas. Add explicit fit-scope provenance and mandatory leakage,
   alignment, orientation, probability, reload, and test-exclusion checks.
3. Rebuild E0 only when a verified local Wikipedia corpus is supplied. Retrieval thresholds and
   classification thresholds are selected exclusively in inner grouped validation.
4. Run E1 on the common folds: exact normalized substring for context-present rows and Candidate
   I for context-absent rows. Tune only the small declared C/threshold grid in inner folds.
5. Run E3 from independently regenerated common-fold E0/E1 probabilities using a small declared
   route-rule set. Do not use the legacy V4-A OOF artifact for blending.
6. Run E2 only when the authenticated pinned offline BanglaBERT checkpoint and evidence resource
   exist. Use official context or accepted retrieved evidence and `[NO_EVIDENCE]`; do not claim
   atomic-claim behavior.
7. Run E4 with outer-fold-local, inner-cross-fitted base predictions, a small logistic-C grid,
   and inner-only threshold selection. Apply the stated promotion gate against common-fold E0.
8. Freeze and run M5 only after all gates pass and the competition test plus all offline resources
   are available. Otherwise preserve V4-A as the competition candidate and record V7 as blocked
   ablation work.

Every command runs from repository root, every probability column names its label orientation,
and no test input participates in folds, fitting, retrieval-threshold selection, calibration,
fusion, or classification-threshold selection.
